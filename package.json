{
  "name": "result-app",
  "version": "1.0.0",
  "description": "a result processing app where students can check their results",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Omoalfa",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
